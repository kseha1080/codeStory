<h1>{{user.firstName}}'s</h1> {{!--This line needs to be fixed.  Display's the name of the user logged in rather than the name of the user who owns the posts--}}
<h4>Profile Page</h4>
{{#if stories}}
<table class="striped">
  <thead>
    <tr>
      <th>Title</th>
      
      <th>Status</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    {{#each stories}}
    <tr>
      <td><a href="/stories/show/{{user.id}}">{{title}}</a></td>
      <td><span class="dash-status">{{status}}</span></td>
      </td>
    </tr>
    {{/each}}
  </tbody>

</table>
{{else}}
  <p class="gugi">This user has no code yet.</p>
{{/if}}