<div class="row">
  <div class="col s12 m10">
    <h3>{{story.title}}
      <small>{{{editIcon story.user.id user.id story.id false}}}</small>
      </h3>
    <div class="card story">
      <div class="card-content">
        <span class="card-title">{{story.date}}</span>
        <span class="">Description: {{story.description}}</span>
        <br>
        <span class="">Category: {{story.category}}</span>
        <br>
        <span class="">Languages Used: {{story.languages}}</span>
        <br>
        <span class="">Github Repository: {{story.repolink}}</span>
        <br>
        <span class="">Code: {{{story.body}}}</span>
      </div>
    </div>
  </div>
  <br><br><br>
  <div class="col s12 m2">
    <div class="card center-align">
      <div class="card-content">
        <span class="card-title">{{story.user.firstName}}{{story.user.lastName}}</span>
        <img src="{{story.user.image}}" class='square responsive-img'>
      </div>
      <div class="card-action">
        {{#each stories.user.id}}
          {{this}}
        {{/each}}
        <a href="/stories/user/{{story.user.id}}">More From {{story.user.firstName}}</a>
      </div>
    </div>
  </div>
</div>